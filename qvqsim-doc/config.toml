[simulation]
# Precision of the simulation output (number of digits)
precision = 3

# Default input format to use if not determined by file extension
default_input_format = "qvqsim"

[logging]
# Logging level (DEBUG, INFO, WARNING, etc.)
level = "INFO"
